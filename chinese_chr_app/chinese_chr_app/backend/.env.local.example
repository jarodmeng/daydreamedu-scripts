# Supabase Auth (Google login)
# Used by the backend to verify the Supabase access token (JWT) via JWKS.
# In Supabase: Project Settings → API → Project URL
SUPABASE_URL=https://your_project_ref.supabase.co
# Supabase JWT audience for signed-in users is typically "authenticated"
SUPABASE_JWT_AUD=authenticated

# Local testing only: bypass auth for 拼音记忆 (pinyin recall) and use this as user_id.
# Set to any non-empty string (e.g. local-dev). Do not set in production.
# PINYIN_RECALL_DEV_USER=local-dev

# Optional: when you use Supabase Postgres from the backend
# In Supabase UI: Connect → Connection string → Transaction pooler
# DATABASE_URL=postgresql://postgres.<PROJECT_REF>:[PASSWORD]@aws-<n>-<region>.pooler.supabase.com:6543/postgres?sslmode=require

# Use database tables instead of JSON files (requires DATABASE_URL). Also enables character view logging for signed-in users.
# USE_DATABASE=true

# Optional: serve 字卡 images from GCS (same as prod). If unset, backend uses local data/png.
# GCS_BUCKET_NAME=chinese-chr-app-images
